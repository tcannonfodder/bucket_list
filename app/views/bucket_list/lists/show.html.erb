<h1>Lists#show</h1>
<%= @list.name %> <%= link_to "Edit", edit_list_path(@list) %> <%= link_to "Delete", list_path(@list), :method => :delete %>
<ul>
<% @list.items.each do |item| %>
	<li><%= item.name %></li>
<% end %>
</ul>